## Release archive convenience

- Added the stable `xray-tui-latest.tar.gz` release asset.
- The README can now install the newest stable release without manually
  entering a version tag.
- The release checksum manifest covers both the versioned archive and the
  stable latest-download alias.
